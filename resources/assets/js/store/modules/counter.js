   
// initial state
// shape: [{ id, quantity }]
const state = {
  count: 0
}
  
// getters
const getters = {
  
}

// actions
const actions = {
  
}

// mutations
const mutations = {
  increment (state) {
      // mutate state
      state.count++
      }
  }


export default {
  state,
  getters,
  actions,
  mutations
}
  
